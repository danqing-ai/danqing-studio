#!/usr/bin/env python3
"""
danqing-video-edit — 视频编辑 CLI。

对应 REST API: POST /api/videos/edits → IVideoEngine.edit()

用法:
    danqing-video-edit --model wan-2.2-ti2v-5b --source-asset-id ast_xxx \
                       --prompt "add rain" --output out.mp4
"""
from __future__ import annotations

import argparse
import os
import sys
from pathlib import Path

_CLI_FILE = Path(__file__).resolve()
PROJECT_ROOT = _CLI_FILE.parent.parent
if sys.platform != "win32":
    _venv_root = PROJECT_ROOT / ".venv"
    _venv_py = _venv_root / "bin" / "python3"
    if _venv_py.is_file() and Path(sys.prefix).resolve() != _venv_root.resolve():
        os.execv(str(_venv_py), [str(_venv_py), str(_CLI_FILE), *sys.argv[1:]])
sys.path.insert(0, str(PROJECT_ROOT))

from backend.cli.video_cli import edit


def main():
    parser = argparse.ArgumentParser(description="DanQing CLI: video editing")
    parser.add_argument("--model", required=True)
    parser.add_argument("--source-asset-id", required=True)
    parser.add_argument("--prompt", required=True)
    parser.add_argument("--negative-prompt", default="")
    parser.add_argument("--steps", type=int, default=None)
    parser.add_argument("--seed", type=int, default=None)
    parser.add_argument(
        "--teacache-mode",
        default=None,
        choices=["none", "auto", "fast", "quality"],
        help="TeaCache step cache (wan/hunyuan animate; registry default if omitted)",
    )
    parser.add_argument("--output", required=True)
    parser.add_argument("--project-root", default=str(PROJECT_ROOT))
    args = parser.parse_args()

    print(f"[cli] model={args.model} prompt={args.prompt!r}")
    edit(
        model=args.model,
        source_asset_id=args.source_asset_id,
        prompt=args.prompt,
        negative_prompt=args.negative_prompt,
        steps=args.steps,
        seed=args.seed,
        teacache_mode=args.teacache_mode,
        output=args.output,
        project_root=Path(args.project_root).resolve(),
    )


if __name__ == "__main__":
    main()
