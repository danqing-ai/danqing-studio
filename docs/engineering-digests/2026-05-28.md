# DanQing Studio — Daily Engineering Digest

**Date range:** 2026-05-27 17:00 UTC → 2026-05-28 17:00 UTC  
**Repo:** danqing-ai/danqing-studio (`main`)

## Key changes

- **High-velocity engine refactor on `main`:** 18 commits, ~165 files (+12,555/−9,462) — no merged PRs; all direct pushes by @luodaijun. Anchor: [6f3094d](https://github.com/danqing-ai/danqing-studio/commit/6f3094d) (net-delete dedup, SDPA governance, UX stack).
- **Codec / pipeline consolidation:** Removed `heartmula/mlx/` wrapper tree; added `vae_codec_registry.py`, `video_codec_registry.py`, `memory_policy.py`, and `progress_bridge.py`; slimmed `image_pipeline.py` / `video_pipeline.py`; new `submit_helpers.py` and `task_dispatch.py` for REST/scheduler DRY.
- **Dual-platform DiT backend dispatch:** Unified `transformer.py` + `transformer_cuda.py` stems across flux1, ltx, wan, cogvideox, fibo, flux2, hunyuan, z_image ([264a7b5](https://github.com/danqing-ai/danqing-studio/commit/264a7b5), [b12678c](https://github.com/danqing-ai/danqing-studio/commit/b12678c)); `tests/engine_unit.py` gates registry stems on dispatch ([72e94c3](https://github.com/danqing-ai/danqing-studio/commit/72e94c3)).
- **Qwen Image CUDA:** `qwen-image` registry now declares `mlx` + `cuda` backends; added `transformer_cuda.py` and `text_encoder_cuda.py` ([3e2d567](https://github.com/danqing-ai/danqing-studio/commit/3e2d567)).
- **Same-day parity regressions fixed:** Flux1/FIBO mflux bench restored after dedup broke param-map and FIBO VAE encode ([00056a1](https://github.com/danqing-ai/danqing-studio/commit/00056a1)); z_image `forward()` delegation fix for `NotImplementedError` in mflux ([558d2d9](https://github.com/danqing-ai/danqing-studio/commit/558d2d9)).
- **SeedVR2 + text-encoder churn:** SeedVR2 scheduler/result/video_restore merged into `job_mlx`; encoders centralized then partially moved back to families ([1e7ca84](https://github.com/danqing-ai/danqing-studio/commit/1e7ca84) → [b0ea971](https://github.com/danqing-ai/danqing-studio/commit/b0ea971)); PyInstaller allowlists updated each pass.
- **Frontend create UX:** Work title on image create, `LivingCanvas`, advanced-params layout change, audio/create and gallery polish, theme tokens ([6f3094d](https://github.com/danqing-ai/danqing-studio/commit/6f3094d) frontend diff).
- **Benchmark:** Dropped `z-image-turbo` diffusers parity case (documented FAIL in AGENTS.md) ([cd47182](https://github.com/danqing-ai/danqing-studio/commit/cd47182)).

## Watchlist

1. **12k+ LOC engine churn with two same-day bench regressions already fixed** — run `make verify-engine-stack` and `make bench-mflux` on MLX/CUDA targets before release; z-image-turbo parity no longer gated in CI.
2. **Correctness PRs #4–#7 still open** (batch delete ordering, >500 task pagination, workspace migration/rebind) — none merged in this window; `main` may still carry those bugs in production paths.
3. **Encoder location oscillation** — common/ centralization followed by family-local moves and stub deletes; watch CUDA pack imports and `check-engine-family-layout` on the next family add.
