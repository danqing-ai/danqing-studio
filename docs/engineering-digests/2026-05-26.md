# DanQing Studio — Daily Engineering Digest

**Date range:** 2026-05-25 17:00 UTC → 2026-05-26 17:00 UTC  
**Repo:** danqing-ai/danqing-studio (`main`)

## Key changes

- **Quiet day on `main`:** No commits on `main` in this window (`git log origin/main --since=2026-05-25T17:00:00Z` empty; GitHub commits API `[]`). Last ship remains May 23 (HeartMuLa/Hunyuan/Wan stack, dev workflow unification, Wan TI2V patchify — [0db8019](https://github.com/danqing-ai/danqing-studio/commit/0db8019)).
- **New consolidated correctness PR ([#6](https://github.com/danqing-ai/danqing-studio/pull/6)):** Opened 2026-05-26 on `cursor/critical-correctness-bugs-fcf4` (+300/−31 across 8 files). Merges scope from open [#4](https://github.com/danqing-ai/danqing-studio/pull/4) and [#5](https://github.com/danqing-ai/danqing-studio/pull/5) into one fix set.
- **Gallery batch delete safety:** `asset_store.delete_batch` now commits SQLite deletes before unlinking files — avoids gallery rows pointing at deleted files if commit fails after `unlink()` ([PR #6](https://github.com/danqing-ai/danqing-studio/pull/6)).
- **Task queue >500 cap:** `TaskScheduler` paginates queued/running reload so tasks beyond the first 500-page are not dropped from the in-memory heap on rebuild/restart.
- **Workspace relocation:** `rebind_workspace_after_relocation()` hot-updates `PathResolver`, asset store, and task store when restart is deferred; `migrate_workspace_data()` rolls back completed moves on partial failure; rebind deadlock on `_init_db` fixed.
- **Tests added:** `tests/engine_unit.py` — batch-delete crash safety, scheduler pagination (501st task), migration rollback, store rebind (per PR body).

## Watchlist

1. **Triplicate PRs (#4, #5, #6)** — close or supersede #4/#5 when landing #6 to avoid conflicting `workspace.py` / `workspace_rebind.py` merges.
2. **Deferred restart still required for engine/model cache** — PR #6 rebinds persistence paths only; users who skip restart may still see stale in-memory model state after workspace move.
3. **Queue soak** — pagination is unit-tested for 501 rows; no load test for sustained cancel/priority churn above the old 500 cap.
