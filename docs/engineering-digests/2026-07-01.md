# DanQing Studio — Daily Engineering Digest

**Date range:** 2026-06-30 17:00 UTC → 2026-07-01 17:00 UTC  
**Repo:** danqing-ai/danqing-studio (`main`)

---

## Slack message

*DanQing Studio — Daily Engineering Digest*  
*2026-06-30 17:00 UTC → 2026-07-01 17:00 UTC*

• **Boogu-Image family shipped on `main`** ([`59f5001`](https://github.com/danqing-ai/danqing-studio/commit/59f5001)) — direct push (no PR). Adds MLX `boogu_image` family (`turbo` + `edit` variants), ~2.9k LOC across engine + registry. User impact: new image models in catalog once bundles are installed. Also hardens bundle manifests with `missing_safetensor_shards()` detection and normalizes version keys via shared `version_labels`.

• **[PR #35](https://github.com/danqing-ai/danqing-studio/pull/35) merged** — frontend UI/UX consistency across create flows. Video/Audio composers now support **Add to Queue** while a task runs (matching Image). i18n gaps closed (toasts, vocal language, style-boost prefix). Avatar page gets proper drawer storage, keyboard shortcuts, and `AssetPicker` for audio. 19 files, +518/−193.

• **[PR #36](https://github.com/danqing-ai/danqing-studio/pull/36) merged** — LoRA training page redesigned to shared `copilot-page` shell (sidebar + main), matching Models/Settings/Chat. Removed ~350 lines of custom rail/footer/tile CSS. Net −364 LOC in `LoraTrainView.vue`.

• **Long-video + LLM improvements** (in Boogu commit) — per-request LLM model for chapter parsing (`backend/engine/llm/service.py`), multi-model gallery filtering in `StudioGalleryFilters`, and T2I provenance panel updates. Contracts extended with `llm_model` field.

• **Critical-fix backlog still open — [PR #38](https://github.com/danqing-ai/danqing-studio/pull/38)** opened today. Re-applies asset-delete ordering, task-queue 500-row cap, workspace migration rollback/rebind, and LoRA path traversal fixes still missing on `main`. Adds new **asset-group delete deadlock** fix (`delete_group()` held store lock while calling `delete()`). +195 lines of unit tests. Not merged.

• **Z-Image LoRA training quality — [PR #37](https://github.com/danqing-ai/danqing-studio/pull/37)** opened today. Fixes base face memorization (σ-distribution mismatch) and turbo skin over-smoothing (min-SNR suppression). Adds `[diag]` logging for train/inference σ schedules. Includes `Wand2` → `MagicStick` icon fix for LoRA train page load.

*Watchlist*
1. **Production bugs still live on `main`** — LoRA dataset routes join `file_path` without containment (`loras.py:481`), asset delete unlinks files before DB commit (`asset_store.py:496`), and `delete_group` can deadlock the API worker. [PR #38](https://github.com/danqing-ai/danqing-studio/pull/38) is ready for review/merge.
2. **Boogu-Image landed without PR review** — large registry diff (+1,104 lines in `models_registry.json`) and new family code pushed directly. Run `make verify-engine-stack` + smoke inference on MLX hardware before promoting bundles.
3. **Overlapping fix PRs** — [PR #30](https://github.com/danqing-ai/danqing-studio/pull/30), [PR #32](https://github.com/danqing-ai/danqing-studio/pull/32), and [PR #38](https://github.com/danqing-ai/danqing-studio/pull/38) all target the same persistence/queue/workspace/LoRA bugs. Close stale duplicates and prioritize #38 (adds deadlock fix + tests).

---

## Evidence

| Item | Source |
|------|--------|
| Boogu-Image commit | [`59f5001`](https://github.com/danqing-ai/danqing-studio/commit/59f5001) @ 2026-07-01T04:53:49Z |
| PR #35 merged | [github.com/.../pull/35](https://github.com/danqing-ai/danqing-studio/pull/35) @ 2026-07-01T06:11:57Z |
| PR #36 merged | [github.com/.../pull/36](https://github.com/danqing-ai/danqing-studio/pull/36) @ 2026-07-01T06:12:33Z |
| PR #37 opened | [github.com/.../pull/37](https://github.com/danqing-ai/danqing-studio/pull/37) @ 2026-07-01T06:32:31Z |
| PR #38 opened | [github.com/.../pull/38](https://github.com/danqing-ai/danqing-studio/pull/38) @ 2026-07-01T11:06:26Z |
| Path traversal on `main` | `loras.py:481` — unbounded `file_path` join; no `resolve_path_under()` in repo |
| Asset delete ordering on `main` | `asset_store.py:496` — `unlink()` before `conn.commit()` at line 507 |
| Group delete deadlock on `main` | `asset_store.py:776-789` — `delete_group()` holds lock, calls `self.delete()` |
| Missing shard detection | `bundle_manifest.py` — `missing_safetensor_shards()` added in Boogu commit |
| 5 commits in window | `git log origin/main --since="2026-06-30T17:00:00Z" --until="2026-07-01T17:00:00Z"` |
