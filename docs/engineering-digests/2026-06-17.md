# DanQing Studio — Daily Engineering Digest

**Date range:** 2026-06-16 17:00 UTC → 2026-06-17 17:00 UTC  
**Repo:** danqing-ai/danqing-studio (`main`)

---

## Slack message

*DanQing Studio — Daily Engineering Digest*  
*2026-06-16 17:00 UTC → 2026-06-17 17:00 UTC*

• **Quiet day on `main`:** no merges and no commits in the last 24h. Latest ship was [PR #11](https://github.com/danqing-ai/danqing-studio/pull/11) (2026-06-13) — LoRA train MLX optimizations (QLoRA, grad checkpoint, DoRA training, VLM audit, latent cache).

• **Open PR #12** ([fix(lora): export DoRA as dense deltas and block dataset path traversal](https://github.com/danqing-ai/danqing-studio/pull/12), opened 2026-06-17): addresses two high-severity issues found in post-merge review of PR #11. DoRA-trained adapters looked valid but produced wrong images because `merge_lora_adapters_common` ignored `dora_magnitude` tensors; fix folds DoRA into dense deltas via new `export_registered_adapter()` in `lora_train_export.py`. Adds `resolve_dataset_image_path()` to block `../../` traversal on `GET /api/loras/datasets/{id}/file/{path}`.

• **Security:** dataset image endpoint on `main` can serve arbitrary workspace files via path traversal until PR #12 merges ([`backend/api/routes/loras.py`](https://github.com/danqing-ai/danqing-studio/blob/main/backend/api/routes/loras.py) — no containment check today).

• **Correctness (LoRA):** users training with `train_type: "dora"` on `main` get adapters that silently fail at inference; PR #12 is the proposed fix (+52 lines of unit tests in `tests/engine_unit.py`).

• **Stale correctness backlog:** [PR #10](https://github.com/danqing-ai/danqing-studio/pull/10) and [#4–#9](https://github.com/danqing-ai/danqing-studio/pulls) remain open (batch-delete ordering, >500 task pagination, workspace migration rollback) — none touched this window.

• **CI:** engine-governance workflow failed on PR #12 (imports/sdpa/docs rules); same pattern of failures on recent `main` pushes — likely pre-existing, but blocks merge confidence.

*Watchlist*
1. **Merge PR #12 urgently** — path traversal is exploitable on any deployment exposing the LoRA dataset API; DoRA training on `main` is producing bad adapters.
2. **Review DoRA/QLoRA inference end-to-end** after merge — export path changed for flux/qwen/z_image dreambooth trainers; run `make verify-engine-stack` on MLX hardware.
3. **Correctness PRs #4–#10 still unmerged** — production may still hit task-queue pagination and asset-delete ordering bugs from the v3 sessions refactor.

---

## Evidence

| Item | Source |
|------|--------|
| No `main` commits since 2026-06-13 | `git log origin/main --since="24 hours ago"` → empty |
| PR #12 opened | [github.com/danqing-ai/danqing-studio/pull/12](https://github.com/danqing-ai/danqing-studio/pull/12) @ 2026-06-17T11:13:03Z |
| DoRA export bug | commit `e058ee0` on `cursor/critical-bug-investigation-8eac`; touches `lora_train_export.py`, `flux_dreambooth_mlx.py`, `qwen_image_dreambooth_mlx.py`, `z_image_dreambooth_mlx.py` |
| Path traversal fix | `dataset_store.py` + `loras.py` route |
| Last merge | [PR #11](https://github.com/danqing-ai/danqing-studio/pull/11) @ 2026-06-13T06:52:09Z |
| CI failure | `engine-governance` run on PR #12 @ 2026-06-17T11:13:07Z — `[imports]`, `[sdpa]`, `[docs]` governance failed |
