# DanQing Studio — Daily Engineering Digest

**Date range:** 2026-07-15 17:01 UTC → 2026-07-16 17:01 UTC  
**Repo:** danqing-ai/danqing-studio (`main`)

## Key changes

- **Frontend theme alignment shipped on `main` ([091d60a](https://github.com/danqing-ai/danqing-studio/commit/091d60a)):** New `stores/theme.ts` centralizes theme state via `@danqing/dq-tokens` (`applyDqTheme`, `THEME_OPTIONS`). Default is **mac** dark; legacy `apple-dark` auto-migrates. Settings and shell components updated; 13 token palettes imported in `main.ts`. User impact: broader theme catalog and consistent DQ-UI conventions across Studio.
- **No merged PRs in window:** Last merge remains [PR #51](https://github.com/danqing-ai/danqing-studio/pull/51) (long-video script-parse pipeline, 2026-07-05). `main` had a single direct commit in this 24h window.
- **[PR #52](https://github.com/danqing-ai/danqing-studio/pull/52) opened (not merged):** Consolidated critical-fix set (+458/−67, 10 files) — asset delete ordering (DB commit before file unlink), task queue pagination past 500-row cap, workspace migration rollback + in-process store rebind, LoRA path traversal guards via `resolve_path_under`. Includes new `workspace_rebind.py` and `tests/engine_unit.py` coverage. Re-applies fixes still missing on `main` after prior investigation PRs (#45, #41, #38, #32, #30).
- **CI governance red on `main`:** [Run 29465011710](https://github.com/danqing-ai/danqing-studio/actions/runs/29465011710) failed on pre-existing engine governance debt (import layout, mlx-torch, pipeline `family ==` branches, extra `docs/` files) — not introduced by the theme commit, but blocks a clean green signal on latest `main`.
- **Stale open PR backlog:** [#45](https://github.com/danqing-ai/danqing-studio/pull/45), [#41](https://github.com/danqing-ai/danqing-studio/pull/41), [#38](https://github.com/danqing-ai/danqing-studio/pull/38), [#32](https://github.com/danqing-ai/danqing-studio/pull/32), [#30](https://github.com/danqing-ai/danqing-studio/pull/30) (overlapping critical-fix scope) and [#26](https://github.com/danqing-ai/danqing-studio/pull/26) (LoRA path traversal) remain open — PR #52 supersedes most of the critical-fix set.

## Watchlist

1. **Merge PR #52 promptly** — asset-delete data loss, queue >500 cap, workspace partial-migration split, and LoRA path traversal are live on `main` until landed; daily investigation branches keep re-applying the same fix set.
2. **Theme rollout regression risk** — `apple-dark` → `mac` migration and 13 new palettes need spot-check on Settings, Studio composer, long-video, and Tauri macOS shell; no automated visual regression tests in this commit.
3. **Governance CI noise** — `make check-engine-rules` failures on `main` mask whether new commits are clean; consider a dedicated frontend-only check or governance cleanup sprint.
