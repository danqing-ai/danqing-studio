# DanQing Studio — Daily Engineering Digest

**Date range:** 2026-05-26 17:00 UTC → 2026-05-27 17:00 UTC  
**Repo:** danqing-ai/danqing-studio (`main`)

## Key changes

- **Quiet day on `main`:** No commits or merged PRs in this window (`git log origin/main --since=2026-05-26T17:00:00Z` empty; GitHub GraphQL `history` on `main` `[]`). Last ship on `main` remains May 23 (HeartMuLa/Hunyuan/Wan stack, dev workflow unification, Wan TI2V patchify — [0db8019](https://github.com/danqing-ai/danqing-studio/commit/0db8019)).
- **New duplicate correctness PR ([#7](https://github.com/danqing-ai/danqing-studio/pull/7)):** Opened 2026-05-27 on `cursor/critical-bug-investigation-97d1`; same tree as open [#6](https://github.com/danqing-ai/danqing-studio/pull/6) (`git diff-tree` empty). Both bundle four high-severity fixes (+300/−31, 8 files) still **not on `main`**.
- **Gallery batch delete safety:** `SQLiteAssetStore.delete_batch` commits DB deletes before unlinking files — prevents gallery rows pointing at deleted files when SQLite commit fails after partial `unlink()` ([#7](https://github.com/danqing-ai/danqing-studio/pull/7)).
- **Task queue >500 cap:** `TaskScheduler` paginates queued/running reload so tasks beyond the first 500-page are not dropped from the in-memory heap on rebuild/restart ([#7](https://github.com/danqing-ai/danqing-studio/pull/7)).
- **Workspace relocation / migration:** `rebind_workspace_after_relocation()` hot-updates `PathResolver`, asset store, and task store when restart is deferred; `migrate_workspace_data()` rolls back completed `shutil.move` steps on partial failure ([#7](https://github.com/danqing-ai/danqing-studio/pull/7)).
- **Unit tests added (branch only):** `tests/engine_unit.py` — batch-delete crash safety, scheduler pagination (501st task), migration rollback, store rebind (per [PR #7](https://github.com/danqing-ai/danqing-studio/pull/7) body).

## Watchlist

1. **Four open PRs (#4–#7) with overlapping workspace/queue fixes** — land one (#6 or #7) and close #4/#5/#6/#7 duplicates to avoid conflicting merges on `workspace.py` / `workspace_rebind.py`.
2. **Correctness fixes still off `main`** — until merged, production retains batch-delete data-loss risk, >500 queued tasks dropped after restart, and split-brain workspace migration/rebind bugs.
3. **Deferred restart limits** — rebind covers persistence paths only; users who skip restart after workspace move may still see stale in-memory model/engine cache (per prior digest / PR notes).
