# DanQing Studio — Daily Engineering Digest

**Date range:** 2026-06-17 17:00 UTC → 2026-06-18 17:00 UTC  
**Repo:** danqing-ai/danqing-studio (`main`)

---

## Slack message

*DanQing Studio — Daily Engineering Digest*  
*2026-06-17 17:00 UTC → 2026-06-18 17:00 UTC*

• **Quiet day on `main`:** no merges and no commits in the last 24h. Latest ship remains [PR #11](https://github.com/danqing-ai/danqing-studio/pull/11) (2026-06-13) — LoRA train MLX optimizations (QLoRA, grad checkpoint, DoRA training, VLM audit, latent cache).

• **Open PR #13** ([fix(security): block LoRA path traversal and harden asset delete](https://github.com/danqing-ai/danqing-studio/pull/13), opened 2026-06-18): expands path-safety beyond PR #12's dataset-only fix. Adds `resolve_path_under()` in `backend/utils/path_utils.py` and applies it to dataset file serve/delete, training artifact serve, and `resume_from` checkpoint validation. Also fixes asset batch-delete ordering — DB commit before file `unlink()` — to prevent gallery data loss on commit failure (`backend/persistence/asset_store.py`). +178/−34 across 5 files; +71 lines of unit tests.

• **Open PR #12 still unmerged** ([fix(lora): export DoRA as dense deltas and block dataset path traversal](https://github.com/danqing-ai/danqing-studio/pull/12), opened 2026-06-17): DoRA-trained adapters on `main` still produce wrong inference results because `merge_lora_adapters_common` ignores `dora_magnitude` tensors; fix folds DoRA into dense deltas via new `export_registered_adapter()` in `lora_train_export.py`. Touches flux/qwen/z_image dreambooth trainers. +234/−79 across 7 files.

• **Security surface on `main`:** LoRA API routes (introduced with PR #11) remain exploitable on network-exposed instances (`0.0.0.0:7800`) — unauthenticated `../../` traversal on dataset images, training artifacts, and unvalidated `resume_from` paths per PR #13 analysis. No `resolve_path_under` or `resolve_dataset_image_path` exists on `main` today.

• **Overlapping fix backlog:** PR #13 and PR #12 both touch `backend/api/routes/loras.py` and `backend/engine/training/dataset_store.py` (independent branches off `main` — merge conflict likely). PR #13's asset-delete fix also overlaps with stale [PR #10](https://github.com/danqing-ai/danqing-studio/pull/10) (batch-delete + task-queue pagination + workspace migration rollback).

• **CI:** `governance` workflow fails on both PR #12 and PR #13 — pre-existing `[imports]` (LoRA training mlx imports), `[sdpa]` (cogview4), and `[docs]` (`docs/plans/` subdirectory) violations on `main`; not introduced by either PR but blocks merge confidence.

*Watchlist*
1. **Merge PR #12 + PR #13 with conflict resolution** — path traversal is live on `main`; DoRA training produces silently broken adapters; asset batch-delete can cause permanent gallery data loss.
2. **Reconcile with PR #10** — three open PRs now touch `asset_store.py` delete ordering; consolidate or sequence merges to avoid regressions.
3. **Run `make verify-engine-stack` on MLX after LoRA fixes land** — export path changed for all three dreambooth trainers; full `engine_unit` suite needs MLX/Pillow venv.

---

## Evidence

| Item | Source |
|------|--------|
| No `main` commits since 2026-06-13 | `git log origin/main --since="2026-06-17T17:00:00Z"` → empty |
| PR #13 opened | [github.com/danqing-ai/danqing-studio/pull/13](https://github.com/danqing-ai/danqing-studio/pull/13) @ 2026-06-18T11:09:44Z |
| PR #13 commit | `20a0869` on `cursor/critical-bug-investigation-a85d` |
| PR #12 still open | [github.com/danqing-ai/danqing-studio/pull/12](https://github.com/danqing-ai/danqing-studio/pull/12) @ 2026-06-17T11:13:03Z |
| No path containment on `main` | `grep resolve_path_under backend/` → no matches |
| Asset delete ordering on `main` | `asset_store.py` unlinks files inside transaction before commit |
| Last merge | [PR #11](https://github.com/danqing-ai/danqing-studio/pull/11) @ 2026-06-13T06:52:09Z |
| CI failure | `governance` run on PR #13 @ 2026-06-18T11:10:01Z — `[imports]`, `[sdpa]`, `[docs]` |
