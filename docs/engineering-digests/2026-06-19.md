# DanQing Studio — Daily Engineering Digest

**Date range:** 2026-06-18 17:00 UTC → 2026-06-19 17:00 UTC  
**Repo:** danqing-ai/danqing-studio (`main`)

---

## Slack message

*DanQing Studio — Daily Engineering Digest*  
*2026-06-18 17:00 UTC → 2026-06-19 17:00 UTC*

• **Quiet day on `main`:** no merges and no commits in the last 24h. Latest ship remains [PR #11](https://github.com/danqing-ai/danqing-studio/pull/11) (2026-06-13) — LoRA train MLX optimizations (QLoRA, grad checkpoint, DoRA training, VLM audit, latent cache).

• **New [PR #14](https://github.com/danqing-ai/danqing-studio/pull/14)** (opened 2026-06-19): consolidates five high-severity correctness fixes still on `main` — asset delete ordering (`asset_store.py` unlinks before `commit()` today), task queue stranded past 500 rows (`task_scheduler.py` `_rebuild_queued_heap`), workspace migration rollback + hot-rebind after relocation (`workspace.py`, new `workspace_rebind.py`), and LoRA path traversal (`loras.py`, `path_utils.py`). +423/−54 across 10 files; +170 lines of unit tests.

• **[PR #13](https://github.com/danqing-ai/danqing-studio/pull/13)** (opened 2026-06-18): narrower security patch — `resolve_path_under()` for LoRA dataset/artifact/`resume_from` paths plus asset batch-delete ordering. Subset of PR #14; both touch `loras.py`, `dataset_store.py`, and `asset_store.py` (merge conflict likely if landed separately).

• **DoRA export bug still unmerged:** [PR #12](https://github.com/danqing-ai/danqing-studio/pull/12) (opened 2026-06-17) — DoRA-trained adapters on `main` still produce wrong inference because `merge_lora_adapters_common` ignores `dora_magnitude`; fix folds into dense deltas via `export_registered_adapter()`. Not updated in this window.

• **Security surface on `main` unchanged:** LoRA API routes remain exploitable on network-exposed instances (`0.0.0.0:7800`) — `grep resolve_path_under backend/` returns no matches on `main`; `asset_store.delete()` still unlinks files inside the transaction before `commit()`.

• **CI:** `governance` workflow failed on PR #14 (2026-06-19) — pre-existing `[imports]` (LoRA training mlx imports), `[sdpa]` (cogview4), and `[docs]` (`docs/plans/` subdirectory) violations on `main`; not introduced by PR #14.

*Watchlist*
1. **Prioritize merging PR #14** — supersedes PR #13 and stale [PR #10](https://github.com/danqing-ai/danqing-studio/pull/10); path traversal, gallery data loss, and queue-stranding bugs are live on `main`.
2. **Land PR #12 separately** — DoRA training silently produces broken adapters until export path is fixed; touches dreambooth trainers, not covered by PR #14.
3. **Run `make verify-engine-stack` on MLX after merges** — PR #14 adds 170 lines of `engine_unit` tests; full suite needs MLX/Pillow venv; governance CI red on unrelated `main` debt.

---

## Evidence

| Item | Source |
|------|--------|
| No `main` commits since 2026-06-13 | `git log origin/main --since="2026-06-18T17:00:00Z"` → empty |
| PR #14 opened | [github.com/danqing-ai/danqing-studio/pull/14](https://github.com/danqing-ai/danqing-studio/pull/14) @ 2026-06-19T11:18:54Z |
| PR #14 commit | `c932c77` on `cursor/critical-bug-investigation-51ed` |
| PR #13 opened (within window) | [github.com/danqing-ai/danqing-studio/pull/13](https://github.com/danqing-ai/danqing-studio/pull/13) @ 2026-06-18T11:09:44Z |
| No path containment on `main` | `grep resolve_path_under backend/` → no matches |
| Asset delete ordering on `main` | `asset_store.py:476-488` unlinks before `commit()` |
| Queue 500-row cap on `main` | `task_scheduler.py:276-279` `list_tasks(limit=500)` |
| Last merge | [PR #11](https://github.com/danqing-ai/danqing-studio/pull/11) @ 2026-06-13T06:52:09Z |
| CI failure PR #14 | `governance` run [27822596149](https://github.com/danqing-ai/danqing-studio/actions/runs/27822596149) @ 2026-06-19 — `[imports]`, `[sdpa]`, `[docs]` |
