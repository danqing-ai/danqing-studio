# DanQing Studio — Daily Engineering Digest

**Date range:** 2026-05-23 17:02 UTC → 2026-05-24 17:02 UTC  
**Repo:** danqing-ai/danqing-studio (`main`)

## Key changes

- **Quiet day on `main`:** No commits landed on `main` in this window (`git log origin/main --since=2026-05-23T17:02:24Z` is empty). Last merge to `main` remains the May 23 daytime stack (HeartMuLa/Hunyuan/Wan, dev workflow unification, Wan TI2V patchify fix).
- **Open PR — persistence & queue correctness ([PR #5](https://github.com/danqing-ai/danqing-studio/pull/5)):** Opened 2026-05-24. Targets three high-severity bugs: (1) `POST /api/assets/batch-delete` could leave DB rows after files were unlinked on crash; (2) `TaskScheduler` dropped queued tasks beyond a 500-row reload cap; (3) `apply-workspace` without restart left in-process stores on the old data root. Adds `workspace_rebind.py` and unit tests in `tests/engine_unit.py`.
- **Open PR — workspace migration safety ([PR #4](https://github.com/danqing-ai/danqing-studio/pull/4)):** Still open. Rolls back partial `migrate_workspace_data()` moves on failure so a mid-move error cannot strand `db/` under the new path while the pointer still references the old root (gallery/task DB loss on next launch).
- **Risk theme:** Both open PRs harden **workspace relocation** and **SQLite/asset consistency** — areas that directly affect gallery, task queue, and settings durability.
- **CI:** No new workflow runs on `main` in the last 24h; most recent `build-desktop` activity on `main` dates to May 17–19 (several failures on Windows/npm paths in that batch).

## Watchlist

1. **Merge & soak PR #5 before promoting workspace changes** — hot-rebind covers PathResolver/asset/task stores but PR notes engine/model cache still expects restart; verify deferred-restart UX in production.
2. **Queue stress** — pagination fix is unit-tested for 501 queued rows; no load test for cancel/priority churn under sustained >500 queue depth.
3. **PR #4 + #5 overlap** — both touch workspace migration/rebind; merge order and conflict check on `backend/utils/workspace.py` vs `workspace_rebind.py`.
