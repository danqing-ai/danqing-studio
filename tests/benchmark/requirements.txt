# Image eval benchmark venv (isolated from project .venv)
# Generation uses project .venv; judge (PickScore) runs in this venv.
# PickScore + transformers<5: see modelscope/evalscope#1203
torch>=2.0
torchvision>=0.15
transformers>=4.40,<5.0
huggingface-hub>=0.23,<2.0
modelscope>=1.20.0
Pillow
numpy
