# Linux / Windows — CUDA backend.
# Includes common deps + CUDA-specific packages.
#
# Install PyTorch with CUDA FIRST (not in this file — varies by CUDA version):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# Then:
#   pip install -r requirements-cuda.txt
-r requirements.txt

# MuQ encoder (DiffRhythm CUDA fallback)
muq
