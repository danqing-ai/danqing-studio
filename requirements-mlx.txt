# macOS Apple Silicon — MLX backend.
# Includes common deps + MLX stack + CPU torch (for ACE-Step logits).
-r requirements.txt

# MLX core
mlx>=0.21.0
mlx-lm
mlx-vlm

# ACE-Step constrained LM (CPU torch for logits; no CUDA needed)
torch>=2.2.0
